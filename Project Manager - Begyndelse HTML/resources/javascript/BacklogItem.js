var BacklogItem = function (id, title, estimat, priority, creator, responseble) {
    this.id = id;
    this.title = title;
    this.estimat = estimat;
    this.priority = priority;
    this.creator = creator;
    this.responseble = responseble;
    this.createdAt;
};